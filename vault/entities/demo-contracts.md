# Entity: the 4 curated demo contracts (seeded 2026-07-10)

Customer entity: **Vista Equity Partners** (as the contract owner/customer).
Owner account: admin@admin.com / admin.

| Contract | id (prefix) |
|---|---|
| Oak3 Executive Search agreement | ca21fa8d… |
| Poppulo MSA | b1d42305… |
| Replit Order Form ($165K) | 265a9516… |
| Replit Commercial Agreement / MSA | 3861cc1a… |

How they were made: uploaded normally, then — because the placeholder OpenAI
key made LLM analysis fail — clause/risk/redline analysis was hand-authored by
Claude and written directly to the DB via a one-off `populate_analysis.py`
script that lived in a session scratchpad and **no longer exists on disk**.

Implications:
- These rows cannot be regenerated by re-uploading; treat as irreplaceable
  (see `demo-data-care` skill; pg_dump before any destructive change).
- `contract_clauses.embedding` is NULL everywhere → semantic search silently
  falls back to keyword matching. Expected, not a bug.
- Re-processing ("reprocess" endpoint) one of these contracts would OVERWRITE
  the curated analysis with heuristic output — don't, without explicit approval.
