# Vault Index — ContractsPulse working memory

Grep this vault before opening pages; pull only what the task needs.

- [decisions/no-docker-stack.md](decisions/no-docker-stack.md) — why the stack
  runs user-space (no Docker/Homebrew) and the exact start commands per service.
- [entities/demo-contracts.md](entities/demo-contracts.md) — the 4 curated
  Vista Equity demo contracts, their ids, how they were populated, what's NULL.
